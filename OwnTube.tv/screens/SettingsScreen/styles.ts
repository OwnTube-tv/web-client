import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "flex-start",
    gap: 10,
    padding: 20,
  },
  option: {
    flexDirection: "row",
    gap: 5,
  },
});
