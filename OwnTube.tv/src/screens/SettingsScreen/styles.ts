import { StyleSheet } from "react-native";

export const styles = (theme: any) =>
  StyleSheet.create({
    container: {
      gap: 10,
      padding: 20,
    },
    option: {
      alignItems: "center",
      flexDirection: "row",
      justifyContent: "space-between",
    },
  });
